extends layout 

block main 
  p Here are your favourite movies:

  ul
  each movie in movies
    p 
    a(href=`/movies/${movie.id}`)= movie.title  
    form(action=`/movies/delete/${movie.id}` method="POST")
      button(type="submit") Delete Movie